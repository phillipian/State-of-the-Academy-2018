mixin grouped_barchart(id, accent, height, url, xLabel, yLabel, labels) 
  div(id = id class = "barchart barchart-grouped" data-url = url data-accent = accent data-height = height data-x = xLabel data-y = yLabel data-labels = labels)
    .tooltip.hidden
  
